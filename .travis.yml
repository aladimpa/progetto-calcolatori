language: c
compiler:
  - clang
  - gcc
env:
  - CFLAGS=-std=gnu99 -Werror -g -ggdb -Wfatal-errors -Wall -Wextra -pedantic -D DEBUG -lpthread
  - CFLAGS=-std=gnu99 -O3 -lpthread
script:
  - make all
  - make test
